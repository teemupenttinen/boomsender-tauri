import { style } from '@vanilla-extract/css'

export const baseViewStyle = style({
  padding: '8px',
})

export const backButtonStyle = style({
  color: 'white',
})

export const baseViewTopContainer = style({
  height: '10px',
})
